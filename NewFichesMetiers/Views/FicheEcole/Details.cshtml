@model NewFichesMetiers.Models.fiche_ecole

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>fiche_ecole</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.fiche.nom)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.fiche.nom)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.ecole.nom)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ecole.nom)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Editer", "Editer", new { id=Model.id }) |
    @Html.ActionLink("Retour à la liste", "Index")
</p>
