@model IEnumerable<NewFichesMetiers.Models.fiche_ecole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fiche.nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ecole.nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fiche.nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ecole.nom)
        </td>
        <td>
            @Html.ActionLink("Editer", "Editer", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
